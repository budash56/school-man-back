Project Scan — Inconsistencies / Possible Errors / Work In Progress

Work in progress
- Certificates report is metadata-only; PDF body/signature blocks are explicitly TODO.
  Files: src/reports/reports.service.ts, docs/backend-overview.md
- Docker/Compose packaging is marked as “under evaluation”.
  File: docs/backend-overview.md

Inconsistencies / possible errors
- `migration:revert` npm script has a typo (`ypeorm-ts-node-commonjs`), so the command will fail.
  File: package.json
- Docs reference a `.env.example` that is “not present yet”, but only `.env` exists.
  File: docs/backend-overview.md
- SQL seed script uses a placeholder password hash (`password_placeholder`), which will not match real credentials if used as-is.
  File: db/seed-teachers-and-courses.sql
- Reports print IDs depend on the `print_generation_seq` sequence. If migrations weren’t applied, the API throws “Failed to obtain next print identifier”.
  Files: src/reports/print-id.service.ts, src/migrations/1760067000000-add_print_generation_seq.ts
